{
  "name": "p2p-file-transfer",
  "version": "1.0.0",
  "description": "Direct device-to-device file transfer tool (no storage limit, no server storage)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
